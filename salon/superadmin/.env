# When true, the admin app uses built-in mock data for the dashboard and
# bookings instead of calling http://localhost:5000/api. Useful for preview
# deploys where the real backend isn't reachable. Set to "false" once your
# backend is deployed and reachable from wherever this app is hosted.
VITE_USE_MOCK_DATA=true
